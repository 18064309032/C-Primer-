#include <bitset>
#include <vector>
#include <iostream>
int main()
{
	std::vector<int> vi{ 1, 2, 3, 5, 8, 13 };
	std::bitset<32> i;
	for (auto v : vi)
		i.set(v);
	std::cout << i << std::endl;
	return 0;
}